# Copy to .env.local for local dev. Real values live in Vercel project settings.
# Never commit .env.local — secrets belong in the host, not the repo.

# Contact form (Phase 4 — Server Action)
# RESEND_API_KEY=
# CONTACT_TO_EMAIL=

# Public site URL, used for absolute OG image URLs
# NEXT_PUBLIC_SITE_URL=https://oakcooper.com
